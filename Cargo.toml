[package]
name = "lua-ffi"
version = "0.1.0"
edition = "2024"

[lib]
name = "lua_ffi"
crate-type = ["cdylib", "staticlib"]

[dependencies]
uniffi = { version = "=0.29.5", features = ["tokio", "cli"] }
